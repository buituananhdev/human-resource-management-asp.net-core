@model List<HRM.Models.Department>
@*<link href="~/Shared/styles.css" rel="stylesheet" type="text/css" />*@

<style>
html,
body {
    height: 100%; /* độ cao của body và html cùng bằng 100% */
    margin: 0; /* bỏ các margin */
    padding: 0; /* bỏ các padding */
    overflow: auto; /* bật tính năng scroll */
    box-sizing: border-box;
}
ul li {
    margin: 0;
    padding: 0;
    list-style: none;
}
.table {
    min-width: 1000px !important;
    width: 100%;
    border-collapse: separate;
    border-spacing: 0 10px;
}
.table tbody tr {
        margin-bottom: 10px;
        cursor: pointer;
}
    .table .thead-dark th {
        color: #fff;
        background-color: #343a40;
        border-color: #454d55;
    }
    .table thead th {
        border: none;
        padding: 30px;
        font-size: 14px;
        color: #fff;
    }
    .table thead th {
        vertical-align: bottom;
        border-bottom: 2px solid #dee2e6;
    }
    .table tbody tr {
        margin-bottom: 10px;
        -webkit-box-shadow: 0px 5px 12px -12px rgba(0, 0, 0, 0.29);
        -moz-box-shadow: 0px 5px 12px -12px rgba(0, 0, 0, 0.29);
        box-shadow: 0px 5px 12px -12px rgba(0, 0, 0, 0.29);
    }
    .table tbody th, 
    .table tbody td {
        border: none;
        padding: 30px;
        font-size: 14px;
        background: #fff;
    }
.alert {
    position: relative;
    padding: 0.75rem 1.25rem;
    margin-bottom: 1rem;
    border: 1px solid transparent;
    border-radius: 0.25rem;
}
.box-add{
    width: 100%;
    text-align: end;
}
.add{
    border: none;
    /* list-style: none; */
    background-color: #fff;
    box-shadow: 3px 3px 10px 2px #d8d3d3;
    border-radius: 5px;
    text-align: end;
    text-decoration: none;
    color: black;
    padding: 5px 10px;
}

</style>
<h2 class="title">Danh sách các phòng ban</h2>
<div class="container">
    <div class="row justify-content-center">
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="table-wrap">
                <div class="box-add">
                    <a href="Department/AddDepartment" class="add">+ Thêm phòng ban</a>
                </div>
                 
                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th>ID Phòng ban</th>
                            <th>Tên phòng ban</th>
                            <th>Chức năng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var department in Model)
                                    {
                                    <tr class="alert">
                                        <td>@department.department_id</td>
                                        <td>@department.department_name</td>
                                        <td>
                                            <a asp-action="UpdateDepartment" asp-route-id="@department.department_id">Cập nhật  </a> <br>
                                                <a asp-action="GetSingleDepartment" asp-route-id="@department.department_id">Danh sách  </a> <br>
                                                <a onclick="confirmAction()"  asp-action="DeleteDepartment" asp-route-id="@department.department_id">
                                              Xóa </a>                                        
                                        </td>
                                     </tr>
                                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<script>
function confirmAction() {
    if (confirm("Bạn có chắc muốn xóa phòng ban này không?")) {
            window.location.href = '@Url.Action("DeleteDepartment", "Department")';
        alert("Bạn đã xóa thành công!");
    }
}
</script>